import { makeProject } from "@motion-canvas/core";
import chunking_semantic_detail from "./scenes/chunking_semantic_detail?scene";

export default makeProject({
  scenes: [chunking_semantic_detail],
});
