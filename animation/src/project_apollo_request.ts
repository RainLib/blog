import { makeProject } from "@motion-canvas/core";
import apollo_request_lifecycle from "./scenes/apollo_request_lifecycle?scene";

export default makeProject({
  scenes: [apollo_request_lifecycle],
});
