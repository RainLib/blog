---
slug: hello-motion-canvas
title: Hello Motion Canvas
authors: [me]
tags: [motion-canvas, demo]
---

import MotionCanvasPlayer from "@site/src/components/MotionCanvasPlayer";

# Welcome to Idea Visual

This is a demonstration of **Motion Canvas** integrated into a Docusaurus blog.

The animation below is rendered in real-time using the `<MotionCanvasPlayer />` component.

<MotionCanvasPlayer src="/animation/src/project.js" />

:::info
Make sure to build the animation project first using `cd animation && npm run build`.
The build output is located at `static/animation`.
:::
